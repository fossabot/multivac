---
/**
 * 密码保护组件 - 纯 HTML 表单
 * 脚本逻辑在 BlogPost.astro 中
 */
interface Props {
    passwordHint?: string;
}

const { passwordHint } = Astro.props;
const defaultHint = '💡 提示：密码由文章作者设置';
import { Icon } from 'astro-icon/components';
---

<style>
    .pwd-field {
        position: relative;
        display: flex;
        align-items: center;
    }
    .pwd-field input {
        -webkit-text-security: disc;
        flex: 1;
        width: 100%;
        padding: 0.75rem 1rem;
        padding-right: 2.75rem;
        border-radius: 0.5rem;
        border: 2px solid var(--border);
        outline: none;
        transition: border-color 0.2s;
    }
    .pwd-field input:focus {
        border-color: var(--primary);
    }
    .pwd-field input.revealed {
        -webkit-text-security: none;
    }
    .pwd-field input::-ms-reveal,
    .pwd-field input::-ms-clear,
    .pwd-field input::-webkit-credentials-auto-fill-button,
    .pwd-field input::-webkit-input-password-toggle {
        display: none !important;
    }
    .pwd-toggle {
        position: absolute;
        right: 0.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        width: 28px;
        height: 28px;
        padding: 0;
        border: none;
        background: none;
        cursor: pointer;
        color: var(--toggle-color);
        -webkit-user-select: none;
        user-select: none;
    }
    .pwd-toggle:hover {
        color: var(--text);
    }
    .pwd-toggle:active {
        transform: none;
    }
    .pwd-toggle svg {
        width: 20px;
        height: 20px;
        pointer-events: none;
    }
    .pwd-toggle .eye-off {
        display: none;
    }
    .pwd-toggle.active .eye-on {
        display: none;
    }
    .pwd-toggle.active .eye-off {
        display: block;
    }
</style>

<div data-password-container class="max-w-md mx-auto mt-16 p-8 bg-card-light dark:bg-card-dark rounded-2xl shadow-xl">
    <div class="text-center mb-6">
        <Icon name="lock" class="w-16 h-16 mx-auto mb-4 text-primary-light dark:text-primary-dark" />
        <div class="text-2xl font-bold text-text-light dark:text-text-dark mb-2">此文章已加密</div>
        <p class="text-text-light/70 dark:text-text-dark/70">请输入密码以查看内容</p>
    </div>

    <form data-password-form action="javascript:void(0)" class="space-y-4">
        <div class="pwd-field">
            <input
                type="text"
                data-password-input
                placeholder="请输入密码"
                autocomplete="off"
            />
            <button type="button" data-toggle-password class="pwd-toggle" aria-label="切换密码可见性">
                <Icon name="eye" class="eye-on" />
                <Icon name="eye-off" class="eye-off" />
            </button>
        </div>

        <div data-error-message class="hidden text-red-500 text-sm text-center"></div>

        <button
            type="submit"
            class="w-full px-6 py-3 bg-primary-light dark:bg-primary-dark text-white rounded-lg font-medium hover:opacity-90 transition-opacity focus:outline-none focus:ring-2 focus:ring-primary-light dark:focus:ring-primary-dark focus:ring-offset-2"
        >
            解锁文章
        </button>
    </form>

    <div class="mt-6 text-center text-sm text-text-light/70 dark:text-text-dark/70">
        <p>{passwordHint || defaultHint}</p>
    </div>
</div>
